## Summary

- **Mini Mode:** Android-style keyboard on single monitor or mirrored display; auto-hide when external input loses focus; 3-button collapsed FAB (Settings → Dictate → Expand); transparent outlined UI toggle; touch/mouse layout snapshots; tool windows centered on the monitor containing main; collapsed FAB sizing with DPI/hover slack; mirror detection via overlapping monitor work areas.
- **Platform:** Remove all macOS support — delete cross-platform stubs, Windows-only CI/releases (`nsis`/`msi`), compile-time guard for non-Windows builds, and update docs. macOS touchscreens are single-point only, which breaks multi-touch layout and monitor targeting this app requires.

## Commit message

Use squash title: `feat: mini mode layout fixes and Windows-only platform`

## Related issue

N/A

## Accessibility impact

- Mini Mode improves usability on single-touch or mirrored setups (common wheelchair-mounted touchscreen + laptop mirror).
- Touch resize drags no longer lose capture mid-gesture.
- Separate touch vs mouse layout ratios persist per input type.

## Test plan

- [x] `npx tsc --noEmit`
- [x] `npm test` (16/16)
- [x] `cargo test` (30/30)
- [ ] Ran `npm run tauri dev` locally
- [ ] Manually tested mini mode on single monitor / mirrored display
- [ ] Manually tested collapsed FAB and transparent mode on device

## Screenshots (if UI changed)

Manual QA on Windows hardware recommended for mini mode shell and transparent UI.
