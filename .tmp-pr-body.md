## Summary

- Long-press tool-window headers (Settings, Macro Builder, Head Tracking) to move undecorated windows; main window stays docked.
- Add transparent keyboard outline color cycle (white / dark gray / silver), shown on the mini-mode toolbar only when transparent mode is on, with a matching Settings control.

## Commit message

`feat: tool-window drag and transparent key colors`

## Related issue

N/A

## Accessibility impact

Tool windows can be repositioned without a native title bar. Transparent keyboard outline color can be switched for contrast against different backgrounds.

## Test plan

- [ ] Ran `npm run tauri dev` locally
- [ ] Long-press Settings / Macro Builder / Head Tracking headers → window moves; close button still works; main window unchanged
- [ ] Enable mini-mode transparent keyboard → color swatch appears; cycle white → dark gray → silver updates key outlines/labels
- [ ] Color control hidden when transparent is off; Settings select matches when transparent is on
- [ ] Updated docs if user-facing behavior changed

## Screenshots (if UI changed)

N/A
